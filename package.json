{
  "name": "taskflow-serverless",
  "version": "1.0.0",
  "description": "Intelligent task management and monitoring platform with React frontend and AWS backend",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "infrastructure"
  ],
  "scripts": {
    "install:all": "npm install && npm run install:frontend && npm run install:backend && npm run install:infra",
    "install:frontend": "cd frontend && npm install",
    "install:backend": "cd backend && npm install",
    "install:infra": "cd infrastructure && npm install",
    "dev": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy:infra": "cd infrastructure && npm run deploy",
    "deploy:backend": "cd backend && npm run deploy",
    "deploy:frontend": "npm run build && npm run deploy:s3",
    "deploy:s3": "node scripts/deploy/deploy-s3.js",
    "deploy:all": "npm run deploy:infra && npm run deploy:backend && npm run deploy:frontend",
    "clean": "node scripts/utils/cleanup.js"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-sdk": "^2.1500.0",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  }
}
